<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>パスワード変更 - LuaAIDiary</title>
    <link rel="stylesheet" href="/static/css/admin.css">
    <style>
        /* パスワード変更ページ専用スタイル */
        .password-change-wrapper {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }

        .password-change-container {
            width: 100%;
            max-width: 500px;
            padding: 20px;
        }

        .password-change-card {
            background-color: var(--card-bg);
            border-radius: 8px;
            padding: 40px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
        }

        .password-change-header {
            text-align: center;
            margin-bottom: 30px;
        }

        .password-change-logo {
            font-size: 48px;
            margin-bottom: 10px;
        }

        .password-change-title {
            font-size: 24px;
            font-weight: 600;
            color: var(--text-primary);
            margin-bottom: 5px;
        }

        .password-change-subtitle {
            font-size: 14px;
            color: var(--text-secondary);
        }

        .password-change-form {
            margin-top: 30px;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-label {
            display: block;
            margin-bottom: 8px;
            font-weight: 500;
            color: var(--text-primary);
            font-size: 14px;
        }

        .form-input {
            width: 100%;
            padding: 12px 15px;
            border: 1px solid var(--border-color);
            border-radius: 4px;
            font-size: 14px;
            font-family: inherit;
            transition: border-color 0.2s, box-shadow 0.2s;
        }

        .form-input:focus {
            outline: none;
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(0, 115, 170, 0.1);
        }

        .btn-submit {
            width: 100%;
            padding: 12px 20px;
            background-color: var(--primary-color);
            color: #ffffff;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.2s;
        }

        .btn-submit:hover {
            background-color: var(--primary-dark);
        }

        .btn-submit:active {
            transform: translateY(1px);
        }

        .password-change-footer {
            margin-top: 20px;
            text-align: center;
            font-size: 13px;
            color: var(--text-secondary);
        }

        .password-change-footer a {
            color: var(--primary-color);
            text-decoration: none;
        }

        .password-change-footer a:hover {
            text-decoration: underline;
        }

        .alert {
            padding: 12px 20px;
            margin-bottom: 20px;
            border-radius: 4px;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .alert-error {
            background-color: #f8d7da;
            border-left: 4px solid var(--error-color);
            color: #721c24;
        }

        .alert-success {
            background-color: #d4edda;
            border-left: 4px solid var(--success-color);
            color: #155724;
        }

        .alert-icon {
            font-size: 18px;
            font-weight: bold;
        }

        .password-requirements {
            background-color: #e7f3ff;
            border-left: 4px solid #2196F3;
            padding: 15px;
            margin-bottom: 20px;
            border-radius: 4px;
        }

        .password-requirements h4 {
            margin: 0 0 10px 0;
            font-size: 14px;
            font-weight: 600;
            color: #1976D2;
        }

        .password-requirements ul {
            margin: 0;
            padding-left: 20px;
            font-size: 13px;
            color: #424242;
        }

        .password-requirements li {
            margin-bottom: 5px;
        }
    </style>
</head>
<body>
    <div class="password-change-wrapper">
        <div class="password-change-container">
            <div class="password-change-card">
                <div class="password-change-header">
                    <div class="password-change-logo">🔐</div>
                    <h1 class="password-change-title">パスワード変更</h1>
                    <p class="password-change-subtitle">新しいパスワードを設定してください</p>
                </div>

                <!-- 成功メッセージ表示 -->
                <% if success_message then %>
                    <div class="alert alert-success">
                        <span class="alert-icon">✓</span>
                        <span><%= success_message %></span>
                    </div>
                <% end %>

                <!-- エラーメッセージ表示 -->
                <% if error_message then %>
                    <div class="alert alert-error">
                        <span class="alert-icon">⚠</span>
                        <span><%= error_message %></span>
                    </div>
                <% end %>

                <!-- パスワード要件の表示 -->
                <div class="password-requirements">
                    <h4>📋 パスワード要件</h4>
                    <ul>
                        <li>8文字以上であること</li>
                        <li>英字（a-z, A-Z）を含むこと</li>
                        <li>数字（0-9）を含むこと</li>
                    </ul>
                </div>

                <!-- パスワード変更フォーム -->
                <form method="POST" action="/admin/change-password" class="password-change-form">
                    <!-- CSRFトークン -->
                    <input type="hidden" name="_csrf_token" value="<%= csrf_token %>">

                    <!-- 現在のパスワード入力 -->
                    <div class="form-group">
                        <label for="current_password" class="form-label">現在のパスワード</label>
                        <input 
                            type="password" 
                            id="current_password" 
                            name="current_password" 
                            class="form-input" 
                            required 
                            autofocus
                            placeholder="現在のパスワードを入力"
                        >
                    </div>

                    <!-- 新しいパスワード入力 -->
                    <div class="form-group">
                        <label for="new_password" class="form-label">新しいパスワード</label>
                        <input 
                            type="password" 
                            id="new_password" 
                            name="new_password" 
                            class="form-input" 
                            required
                            placeholder="新しいパスワードを入力"
                            minlength="8"
                        >
                    </div>

                    <!-- 新しいパスワード確認入力 -->
                    <div class="form-group">
                        <label for="confirm_password" class="form-label">新しいパスワード（確認）</label>
                        <input 
                            type="password" 
                            id="confirm_password" 
                            name="confirm_password" 
                            class="form-input" 
                            required
                            placeholder="新しいパスワードを再入力"
                            minlength="8"
                        >
                    </div>

                    <!-- 変更ボタン -->
                    <button type="submit" class="btn-submit">パスワードを変更</button>
                </form>

                <div class="password-change-footer">
                    <p><a href="/admin/dashboard">← ダッシュボードに戻る</a></p>
                </div>
            </div>
        </div>
    </div>

    <!-- フラッシュメッセージ自動非表示 & パスワード確認 -->
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // フラッシュメッセージ自動非表示
            const alerts = document.querySelectorAll('.alert');
            alerts.forEach(function(alert) {
                setTimeout(function() {
                    alert.style.opacity = '0';
                    alert.style.transition = 'opacity 0.3s';
                    setTimeout(function() {
                        alert.style.display = 'none';
                    }, 300);
                }, 5000);
            });

            // パスワード一致確認
            const form = document.querySelector('.password-change-form');
            const newPassword = document.getElementById('new_password');
            const confirmPassword = document.getElementById('confirm_password');

            form.addEventListener('submit', function(e) {
                if (newPassword.value !== confirmPassword.value) {
                    e.preventDefault();
                    alert('新しいパスワードと確認用パスワードが一致しません。');
                    confirmPassword.focus();
                    return false;
                }

                // パスワード要件チェック（英字+数字）
                const password = newPassword.value;
                const hasLetter = /[a-zA-Z]/.test(password);
                const hasNumber = /[0-9]/.test(password);

                if (!hasLetter || !hasNumber) {
                    e.preventDefault();
                    alert('パスワードには英字と数字の両方を含める必要があります。');
                    newPassword.focus();
                    return false;
                }
            });
        });
    </script>
</body>
</html>
